services:
  db:
    image: postgres:16-alpine
    restart: always
    environment:
      POSTGRES_USER: sklep
      POSTGRES_PASSWORD: sklep_dev_password
      POSTGRES_DB: sklep_nextjs
    ports:
      - "5455:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
